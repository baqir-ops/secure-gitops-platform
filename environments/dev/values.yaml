replicaCount: 1

image:
  repository: 950165721116.dkr.ecr.ap-south-1.amazonaws.com/secure-gitops-app
  tag: "sha-ff08b9c42533e1611d91882d75bc3fbf01d7fd67"
  digest: ""
  pullPolicy: IfNotPresent

config:
  environment: dev
  forceFailure: "false"

autoscaling:
  enabled: false
  minReplicas: 1
  maxReplicas: 3
  targetCPUUtilizationPercentage: 70

serviceMonitor:
  # Enabled later after Prometheus Operator installs the ServiceMonitor CRD.
  enabled: false
  labels:
    release: monitoring

ingress:
  # Kept disabled to avoid creating an AWS load balancer during initial testing.
  enabled: false
